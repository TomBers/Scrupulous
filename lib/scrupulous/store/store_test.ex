defmodule StoreTest do

  def run(keys) do
#    pid = Store.pid
#    keys |> Enum.map(fn(key) -> Store.put(pid, key, 1..100000 |> Enum.map(fn(x) -> Enum.random(0..x) end)) end)
#    Store.calc_memory_mb(pid)
  end

end
